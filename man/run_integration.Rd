% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_run.R
\name{run_integration}
\alias{run_integration}
\title{Run multi-sample integration (dispatcher)}
\usage{
run_integration(
  integration_method = "RISC",
  sample_metadata,
  sobjlist = NULL,
  mdlist = NULL,
  tmpobjdir = NULL,
  datadir = NULL,
  outdir_int,
  outdir_indi = NULL,
  pcs_int = 30L,
  res_int = 0.5,
  risc_reference = "autoV2",
  RISC_louvain_neighbors = 10L,
  workernum = 1L,
  stability_workernum = NULL,
  input_seurat_obj = FALSE,
  stability_outdir = NULL,
  stability_numreps = 50L,
  stability_sweep_maxPCs = c(5, 10, 15, 20, 25, 30, 40, 50),
  stability_sweep_res = seq(0.1, 1.5, by = 0.2),
  stability_propcells.perrep = 0.8,
  pipeline_outdir = NULL
)
}
\arguments{
\item{integration_method}{One of \code{integration_method_choices()}; default \code{"RISC"}.}

\item{sample_metadata}{data.frame with Sample, Condition, Code.}

\item{sobjlist}{Named list of per-sample Seurat objects (for prep), or NULL if using \code{tmpobjdir}.}

\item{mdlist}{Named list of metadata per sample; required for RISC.}

\item{tmpobjdir}{Path to saved per-sample objects (\code{Code.rds}).}

\item{datadir}{Cell Ranger folder; used only when \code{input_seurat_obj = FALSE} (h5 at prep).}

\item{outdir_int}{Integrated analysis output folder.}

\item{outdir_indi}{Individual-sample output folder.}

\item{pcs_int}{PCs for integration, or \code{"auto"} for stability-based selection.}

\item{res_int}{Louvain resolution, or \code{"auto"}.}

\item{risc_reference}{RISC reference: \code{"autoV2"} (default; InPlot-faithful),
\code{"auto"} / \code{"autoV1"} (legacy heuristic), a sample Code/Sample name,
or \code{NULL} (same as \code{"autoV2"}).}

\item{RISC_louvain_neighbors}{Louvain neighbors (RISC only).}

\item{workernum}{Parallel workers for final RISC integration (\code{InPlot}, \code{scMultiIntegrate}).}

\item{stability_workernum}{Parallel workers for auto stability sweep; \code{NULL} uses \code{workernum}.}

\item{input_seurat_obj}{If \code{TRUE}, RISC counts come from \code{sobjlist} at prep (not datadir).}

\item{stability_outdir}{Output directory for stability sweep (default \code{outdir_int/cluster_stability}).}

\item{stability_numreps}{Bootstrap replicates when \code{pcs_int} or \code{res_int} is \code{"auto"}.}

\item{stability_sweep_maxPCs}{PC grid for auto \code{pcs_int}.}

\item{stability_sweep_res}{Resolution grid for auto \code{res_int}.}

\item{stability_propcells.perrep}{Cell fraction per bootstrap replicate.}

\item{pipeline_outdir}{Pipeline root outdir for \code{.scdapp_resume/} (default: parent of \code{outdir_int}).}
}
\value{
List with \code{sobjint}, \code{int_clust_lab}, \code{ip}, \code{config}.
For RISC, also \code{refscore} (legacy auto named vector or NULL),
\code{selected_risc_reference}, and \code{risc_reference_selection}.
All three are \code{NULL} for Seurat methods.
When auto tuning was used, includes \code{stability} and \code{auto_selection}.
}
\description{
Run multi-sample integration (dispatcher)
}
