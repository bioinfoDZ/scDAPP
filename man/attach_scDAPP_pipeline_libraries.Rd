% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attach_pipeline_libraries.R
\name{attach_scDAPP_pipeline_libraries}
\alias{attach_scDAPP_pipeline_libraries}
\title{Attach scDAPP pipeline dependencies in a safe order}
\usage{
attach_scDAPP_pipeline_libraries(
  load_risc = FALSE,
  set_seed = 2022L,
  configure_parallel = TRUE,
  future_globals_max_size = 15000 * 1024^2,
  quietly = FALSE
)
}
\arguments{
\item{load_risc}{logical; attach RISC when TRUE (pipeline default when
\code{integration_method} is RISC).}

\item{set_seed}{optional seed passed to \code{\link[=set.seed]{set.seed()}}; \code{NULL} skips seeding.}

\item{configure_parallel}{if TRUE, call \code{\link[=set_parallel_blas_threads]{set_parallel_blas_threads()}} and set
\code{options(future.globals.maxSize = future_globals_max_size)}.}

\item{future_globals_max_size}{value for \code{future.globals.maxSize} when
\code{configure_parallel} is TRUE.}

\item{quietly}{if TRUE, suppress package startup messages.}
}
\value{
Invisibly, a named character vector of package versions for packages
that were requested to attach.
}
\description{
Loads packages in the order used by
\url{inst/rmd/scRNAseq_clustering_integration.Rmd}:
tidyverse and patchwork first, optional RISC, then Seurat (so Seurat wins
namespace conflicts such as \code{DimPlot}), then scDAPP and remaining dependencies.
}
\details{
Call this before running the pipeline Rmd, smoke scripts, or interactive tests
that rely on Seurat/tidyverse. \code{devtools::load_all()} alone is not sufficient.
}
\examples{
\dontrun{
scDAPP::attach_scDAPP_pipeline_libraries(load_risc = FALSE)
}
}
\seealso{
\code{\link[=r_package_test]{r_package_test()}} for install verification with the same attach order.
}
