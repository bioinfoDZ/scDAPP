% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_stability.R
\name{auto_integration_cluster_params}
\alias{auto_integration_cluster_params}
\title{Select integration PCs and resolution via cluster stability}
\usage{
auto_integration_cluster_params(
  integration_method,
  sample_metadata,
  pcs_int = "auto",
  res_int = "auto",
  tmpobjdir = NULL,
  mdlist = NULL,
  matlist_path = NULL,
  datadir = NULL,
  outdir,
  numreps = 50L,
  propcells.perrep = 0.8,
  sweep_maxPCs = c(5, 10, 15, 20, 25, 30, 40, 50),
  sweep_res = seq(0.1, 1.5, by = 0.2),
  workernum = 1L,
  stability_workernum = NULL,
  risc_reference = "autoV2",
  RISC_louvain_neighbors = 10L,
  input_seurat_obj = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{integration_method}{One of \code{integration_method_choices()}.}

\item{sample_metadata}{data.frame with Sample, Condition, Code.}

\item{pcs_int}{\code{"auto"} or fixed integer (used when not auto).}

\item{res_int}{\code{"auto"} or fixed numeric.}

\item{tmpobjdir}{Per-sample Seurat RDS directory (Seurat methods).}

\item{mdlist}{Named metadata lists (RISC).}

\item{matlist_path}{\code{.concatmatrix.rds} from prep (RISC when \code{input_seurat_obj = TRUE}).}

\item{datadir}{Raw h5 path (RISC when \code{input_seurat_obj = FALSE}).}

\item{outdir}{Stability output directory.}

\item{numreps}{Bootstrap replicates (default 50).}

\item{propcells.perrep}{Cell fraction per bootstrap (default 0.8).}

\item{sweep_maxPCs}{PC grid when \code{pcs_int = "auto"}.}

\item{sweep_res}{Resolution grid when \code{res_int = "auto"}.}

\item{workernum}{Pipeline workers (used for final RISC integration and RISC ncore when sweep is serial).}

\item{stability_workernum}{Parallel workers for sweep \code{foreach} stages; \code{NULL} uses \code{workernum}.}

\item{risc_reference}{Optional RISC reference: \code{"autoV2"} (default),
\code{"auto"} for the legacy heuristic, or a sample Code/Sample name.}

\item{RISC_louvain_neighbors}{RISC Louvain neighbors.}

\item{input_seurat_obj}{If \code{TRUE}, use \code{matlist_path}/\code{tmpobjdir} not \code{datadir}.}

\item{verbose}{Logical.}
}
\value{
List with \code{pcs_int}, \code{res_int}, \code{stability}, \code{sweep_dir},
and for RISC \code{selected_risc_reference} plus \code{risc_reference_selection}.
}
\description{
Select integration PCs and resolution via cluster stability
}
