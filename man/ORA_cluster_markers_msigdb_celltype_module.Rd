% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_pipeline_modules.R
\name{ORA_cluster_markers_msigdb_celltype_module}
\alias{ORA_cluster_markers_msigdb_celltype_module}
\title{ORA of cluster markers against MSigDB cell-type signatures}
\usage{
ORA_cluster_markers_msigdb_celltype_module(
  marker_results,
  pathways,
  context_label,
  outdir,
  marker_padj_thres = 0.05,
  top_markers_per_cluster = 100L,
  min_genes = 7L,
  pathway_padj_thres = 0.1,
  dotplot_top_n = 5L,
  workernum = 1L,
  cp.font.size = 5
)
}
\arguments{
\item{marker_results}{data.frame from \code{Seurat::FindAllMarkers()} with \code{score} column.}

\item{pathways}{output of \code{preppathways_pathwayanalysis_crosscondition_module()}.}

\item{context_label}{string label for outputs (e.g. sample Code or \code{"integrated"}).}

\item{outdir}{directory; writes to \verb{\{outdir\}/celltype_marker_prediction/\{context_label\}/}.}

\item{marker_padj_thres}{adjusted p-value cutoff for marker genes (default 0.05).}

\item{top_markers_per_cluster}{max markers per cluster after padj filter (default 100).}

\item{min_genes}{minimum genes required to run ORA per cluster (default 7).}

\item{pathway_padj_thres}{q-value cutoff passed to \code{enricher()} (default 0.1).}

\item{dotplot_top_n}{top pathways per cluster for dotplot when many are significant (default 5).}

\item{workernum}{number of parallel workers (default 1).}

\item{cp.font.size}{axis text size for dotplot (default 5).}
}
\value{
List with \code{ora_results} (data.frame), \code{dotplot} (ggplot or NULL), and \code{context_label}.
}
\description{
Runs \code{clusterProfiler::enricher()} on cluster marker genes (top N by score among
markers passing \code{marker_padj_thres}) against MSigDB cell-type gene sets from a prepared
msigdbr table. Saves CSV tables and a summary dotplot with inclusive
top-pathway filtering per cluster (same idea as GSEA summary dotplots).
}
