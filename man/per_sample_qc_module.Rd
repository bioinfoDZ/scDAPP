% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_processing.R
\name{per_sample_qc_module}
\alias{per_sample_qc_module}
\title{Per-sample QC filtering module for the scDAPP pipeline}
\usage{
per_sample_qc_module(
  sobj,
  code,
  min_num_UMI,
  min_num_Feature,
  max_perc_mito,
  max_perc_hemoglobin,
  autofilter_complexity,
  autofilter_mito,
  autofilter_nUMI,
  autofilter_medianabsolutedev_threshold,
  autofilter_loess_negative_residual_threshold,
  doubletFinder = TRUE,
  cluster_unfiltered = FALSE,
  qctmp_path,
  rawobj_path = NULL,
  qc_pdf_path,
  prefilter_pcs = 30L,
  prefilter_resolution = 0.1
)
}
\arguments{
\item{sobj}{Seurat object for one sample (with QC metadata)}

\item{code}{sample code used in output file names}

\item{min_num_UMI, min_num_Feature, max_perc_mito, max_perc_hemoglobin}{passed to \code{\link[=autofilter]{autofilter()}}}

\item{autofilter_complexity, autofilter_mito, autofilter_nUMI}{logical; map to
\code{globalfilter.complexity}, \code{globalfilter.mito}, and \code{globalfilter.libsize} in \code{\link[=autofilter]{autofilter()}}}

\item{autofilter_medianabsolutedev_threshold, autofilter_loess_negative_residual_threshold}{passed to \code{\link[=autofilter]{autofilter()}} as \code{mad.score.threshold} and \code{loess_negative_residual_threshold}}

\item{doubletFinder}{logical; run DoubletFinder on autofiltered cells}

\item{cluster_unfiltered}{logical; if TRUE, SCT+cluster the full matrix
before autofilter (default FALSE)}

\item{qctmp_path}{path to save filtered object RDS (overwrites input tmp)}

\item{rawobj_path}{optional path to save unfiltered annotated object RDS; written
only when \code{cluster_unfiltered} is TRUE and this is non-NULL}

\item{qc_pdf_path}{path for per-sample QC summary PDF}

\item{prefilter_pcs}{number of PCs for pre-filter / DoubletFinder clustering}

\item{prefilter_resolution}{Louvain resolution for pre-filter clustering}
}
\value{
list with components \code{af} (autofilter result plus plots) and
\code{rawmd} (metadata from the unfiltered object with filter annotations)
}
\description{
Orchestrates optional pre-filter clustering, automated filtering via
\code{\link[=autofilter]{autofilter()}}, optional DoubletFinder, diagnostic plots, and saving
filtered and unfiltered objects.
}
