% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration_stability.R
\name{integration_stability_plots_module}
\alias{integration_stability_plots_module}
\title{Integration cluster-stability visualization module}
\usage{
integration_stability_plots_module(
  stability = NULL,
  stability_dir = NULL,
  outdir = NULL,
  selected_params_i = NULL,
  label_top_k = 10L,
  save = !is.null(outdir),
  width = 10,
  height = 7
)
}
\arguments{
\item{stability}{Optional list returned by \code{cluster_stability_sweep()}
(or a subset with the three main tables).}

\item{stability_dir}{Directory containing stability CSV exports; used when
\code{stability} is \code{NULL}.}

\item{outdir}{If \code{save = TRUE}, PDFs are written here (created if needed).}

\item{selected_params_i}{\code{params_i} id for the winner (e.g.
\code{"PCs_1-10.res_0.3"}). When \code{NULL}, uses the row with
\code{MAX == "*"} or the highest \code{combinedscore}.}

\item{label_top_k}{Number of top-scoring combinations to label on the ARI vs
Jaccard scatter (default 10).}

\item{save}{Logical; write PDFs to \code{outdir}. When \code{TRUE}, PDF width and
height are computed from the sweep grid (see \code{?cluster_stability_sweep} docs).}

\item{width, height}{Ignored when \code{save = TRUE} (reserved for compatibility).}
}
\value{
List with \code{plots} (named ggplot objects), \code{plot_paths}
(when saved), and \code{selected_params_i}.
}
\description{
Builds diagnostic plots from \code{cluster_stability_sweep()} tables:
ranked combined-score bar chart, PC x resolution heatmap, ARI vs Jaccard
scatter (top combinations labeled), bootstrap ARI boxplots, reference cluster
counts, and a per-cluster Jaccard heatmap for the selected parameter
combination only.
}
